const React = require('react-native');
const { StyleSheet, Dimensions } = React;
import deviceResolution from '../../deviceResolution'

export default {
    listCon:{
        padding: 0,
        height: deviceResolution.heightNoHeader - 50
    }
   
}